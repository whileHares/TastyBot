stages:
  - build
  - test

build-java:
  stage: build
  image: maven:3.8.4-openjdk-17  # Änderung der Maven-Version
  script:
    - echo "Compiling"
    - mvn clean install
  artifacts:
    paths:
      - ./target/*.jar
    expire_in: 2 week

test-java:
  stage: test
  image: maven:3.8.4-openjdk-17  # Änderung der Maven-Version
  script:
    - echo "Running tests"
    - mvn test
